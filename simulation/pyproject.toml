[project]
name = "molecules-and-electrons-sim"
version = "0.1.0"
description = "Replicator-with-learning dynamics, the petrostate harvest-vs-extend valuation, and the magnitude repair for Molecules and Electrons"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "matplotlib>=3.8",
]
